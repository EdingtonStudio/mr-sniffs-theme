<article class="narrow-page">
  <header class="narrow-page__header">
    <h1 class="narrow-page__title">{{ article.title }}</h1>
  </header>
  <div class="narrow-page__body">
    {{ article.content }}
  </div>
</article>
